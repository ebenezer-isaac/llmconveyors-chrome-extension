// SPDX-License-Identifier: MIT
/**
 * Re-export of the canonical storage-key constants. Kept in its own module so
 * tests can import the strings without pulling in the whole config surface.
 */

export { STORAGE_KEYS } from '../config';
