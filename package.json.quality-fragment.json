{
  "scripts": {
    "compliance": "pnpm typecheck && pnpm lint && pnpm test && pnpm validate:blueprints && pnpm validate:grep-gates",
    "validate:blueprints": "tsx scripts/validate-blueprints.ts",
    "validate:grep-gates": "tsx scripts/validate-grep-gates.ts",
    "validate:protocol-schema": "tsx scripts/generate-protocol-schema.ts --check",
    "generate:protocol-schema": "tsx scripts/generate-protocol-schema.ts",
    "tinker": "vite --config tests/harness/vite.config.ts",
    "rigor:full": "pnpm compliance && pnpm validate:protocol-schema"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "zod-to-json-schema": "^3.24.1",
    "ts-morph": "^24.0.0",
    "@playwright/test": "^1.48.2"
  }
}
